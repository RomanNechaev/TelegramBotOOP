package first.games.whowantotbeamillionaire;

import first.IScheme;
import first.ISchemeLoader;

public class MillionaireSchemeLoader<TSource, TResult> implements ISchemeLoader<TSource, TResult> {
    @Override
    public void load(IScheme<TSource, TResult> scheme) {

    }
}
